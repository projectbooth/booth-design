# Client-side OIDC PKCE flow (ADR 0032). Same values booth-core is configured with —
# these are public, non-secret (no client secret exists for this public SPA client).
VITE_OIDC_ISSUER_URL=https://keycloak.example.com/realms/booth
VITE_OIDC_CLIENT_ID=booth-design
